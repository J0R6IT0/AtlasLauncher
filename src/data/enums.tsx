export enum Pages {
    Home,
    Library,
    New,
    Modpacks,
    Settings,
}

export enum Flavours {
    Vanilla,
    Forge,
    Fabric,
    Quilt,
}

export enum EventStatus {
    Loading = 'Loading',
    Success = 'Success',
    Error = 'Error',
}
